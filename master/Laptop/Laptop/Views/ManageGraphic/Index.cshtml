@model IEnumerable<Laptop.Models.Graphic>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}



@Html.ActionLink("Tạo mới", "Create")
<table id="dataTables-example" class="table table-responsive">

    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.graphicName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.manufacturer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.graphicType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.isNoLonger)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.graphicName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.manufacturer)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.graphicType)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.isNoLonger)
                </td>
                <td>
                    @Html.ActionLink("Cập nhật", "Edit", new { id = item.graphicID }) |
                    @Html.ActionLink("Chi tiết", "Details", new { id = item.graphicID }) |
                    @Html.ActionLink("Xóa", "Delete", new { id = item.graphicID })
                </td>
            </tr>
        }
    </tbody>
</table>
